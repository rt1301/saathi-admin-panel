# Node-JWT-Auth-Template
A starter template for Authentication using Node.js, Express, MongoDB and JWT token authorization

## Guide to set up the Repo locally
* Clone the repo to your local system
* Run `npm install` to install all the node modules
* Create an `.env` file similar to given in `.env.example` file
* Run `npm run start` in your command line to start the Nodejs and MongoDB server
